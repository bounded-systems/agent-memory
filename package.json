{
  "name": "@bounded-systems/agent-memory",
  "version": "0.0.0",
  "type": "module",
  "description": "Agent memory as a capability — a dolt-server-backed KV store behind a `memory` door. Fully separate from Front Desk; the beads-memory replacement.",
  "engines": { "node": ">=23.6" },
  "bin": { "agent-memory": "scripts/memory.ts" },
  "scripts": {
    "memory": "node scripts/memory.ts",
    "memoryd": "node scripts/memoryd.ts",
    "test": "node --test test/*.test.ts"
  },
  "dependencies": {
    "mysql2": "^3.11.0",
    "zod": "^4.4.3",
    "@bounded-systems/verbspec": "npm:@jsr/bounded-systems__verbspec@^0.4.0",
    "@bounded-systems/verbspec-mcp": "npm:@jsr/bounded-systems__verbspec-mcp@^0.1.0",
    "@bounded-systems/guest-room": "npm:@jsr/bounded-systems__guest-room@^0.5.0"
  },
  "devDependencies": { "@types/node": "^26.1.1" }
}
